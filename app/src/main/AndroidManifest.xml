<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="ru.spbhse.bingochgk">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.TopicQuestionActivity"></activity>
        <activity android:name=".activities.CollectionQuestionActivity" />
        <activity android:name=".activities.RandomQuestionActivity" />
        <activity android:name=".activities.AllTopicsActivity" />
        <activity android:name=".activities.CreateCollectionActivity" />
        <activity android:name=".activities.ArticleActivity" />
        <activity android:name=".activities.ReferenceActivity" />
        <activity android:name=".activities.CreateArticleActivity" />
        <activity android:name=".activities.SavedQuestionsActivity" />
        <activity android:name=".activities.QuestionActivity" />
        <activity android:name=".activities.ConcreteCollectionActivity" />
        <activity android:name=".activities.CollectionsActivity" />
        <activity android:name=".activities.MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>